package sg.edu.np.mad.teampk.stufftrek;

public class EmptyClass {
// class for notes or removed widgets in case required again

    // retrieved from LocationActivity, replaced with BottomSheetDialog

                /* TO REMOVE THE COMMENT WHEN CreateLocationActivity is created.
                Intent createLocationActivity = new Intent(LocationActivity.this, CreateLocationActivity.class);
                startActivity(createLocationActivity);

                AlertDialog.Builder builder = new AlertDialog.Builder(LocationActivity.this);
                ViewGroup viewGroup = findViewById(android.R.id.content);
                View dialogView = LayoutInflater.from(v.getContext()).inflate(R.layout.dialog_create, viewGroup, false);
                builder.setView(dialogView);
                AlertDialog alertDialog = builder.create();

                Window window = alertDialog.getWindow();
                WindowManager.LayoutParams params = window.getAttributes();
                params.gravity = Gravity.BOTTOM;
                params.y = -100;
                params.width = FILL_PARENT;
                window.setLayout(WindowManager.LayoutParams.MATCH_PARENT, WindowManager.LayoutParams.MATCH_PARENT);
                alertDialog.show();
                 */

}
